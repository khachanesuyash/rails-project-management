<!DOCTYPE html>
<html>
  <head>
    <title>ToDoProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
      
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <h5><%= link_to "Home", root_url, class: "nav-link"%>
              </li>
              
              <% if user_signed_in? %>
                <li class="nav-item">
                    <h5><%= link_to "My Projects", projects_url, class: "nav-link" %>
                </li>
              <% else %>
                <li class="nav-item">
                  <h5><%= link_to "Projects", projects_url, class: "nav-link" %>
                </li>
              <% end %>
              
              
            </ul>
            <ul class="navbar-nav mb-2 mb-lg-0 d-flex">
              <div class="d-flex">
                
                <% if user_signed_in? %>
                  <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown"><%= User.find(current_user.id).name %></a>
                    <div class="dropdown-menu dropdown-menu-end">
                        <p class="dropdown-item">Hello, <%= User.find(current_user.id).name %></p>
                        <%= link_to "Change Password", edit_user_registration_path, class: "dropdown-item" %>
                        <div class="dropdown-divider"></div>
                        <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
                    </div>
                  </li>                
                <% else %>
                  <h5><%= link_to "Log In", new_user_session_path, class: "nav-link"%>
                  <h5><%= link_to "Sign Up", new_user_registration_path, class: "nav-link"%>
                <% end %>
              </div>
            </ul>
          </div>
        </div>
      </nav>
      <div class="container">
        <p class="alert"><%= alert %></p>
        <%= yield %>
      </div>
    </div>
  </body>
</html>

